core:
  os: darwin
  dotfiles_location: ./configs
  installation_command: brew
  shell: zsh

dotfiles:
  - osx:
      os: "darwin"
      files:
        - source: osx/osx
          destinations: ~/.osx

dependencies:
  simples:
    - git
