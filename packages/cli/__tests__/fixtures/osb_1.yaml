# dotfiles: dotfiles location should be provided in order to process dotfiles link
core:
  os: linux
  dotfiles_location: ./wrong-location
  installation_command: apt
  shell: zsh

dotfiles:
  - osx:
      files:
        - source: osx/osx
          destinations: ~/.osx

dependencies:
  simples:
    - git
